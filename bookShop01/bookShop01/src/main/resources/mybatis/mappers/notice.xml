<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.notice">
	<!-- 작성 -->
	<insert id="write">
		insert into t_notice(n_bno, n_title, n_content, n_writer)
		values(t_notice_seq.nextval, #{n_title}, #{n_content}, #{n_writer})	
	</insert>
	
	<!-- 조회 -->
	<select id="read" resultType="com.bookshop01.notice.vo.NoticeVO">
		select n_bno, n_title, n_content, n_writer, n_regDate, n_viewCnt
		 from t_notice 
		 where n_bno= #{n_bno}
	</select>
	
	 <!-- 수정 --> 
	 <update id="update">
	  update t_notice
	   set
	    n_title = #{n_title},
	    n_content = #{n_content}
	   where n_bno = #{n_bno}  
	 </update>
 
	 <!-- 삭제 -->
	 <delete id="delete">
	  delete from
	   t_notice
	  where n_bno = #{n_bno}
	 </delete>
  


  
</mapper>